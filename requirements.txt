# --- Core --------------------------------------------------------------
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0

# --- UI ------------------------------------------------------------------
streamlit>=1.32,<2.0

# --- Local LLM backend (google/flan-t5-base) ----------------------------
# Optional at runtime: if these aren't installed, the app automatically
# falls back to the dependency-free RuleBasedBackend (see coffee_agent/llm).
transformers>=4.38,<5.0
torch>=2.2,<3.0
sentencepiece>=0.2

# --- Testing --------------------------------------------------------------
pytest>=8.0,<9.0
